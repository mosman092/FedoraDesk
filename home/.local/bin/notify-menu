#!/usr/bin/env bash
mapfile -t orig
((${#orig[@]})) || exit 0
disp=()
for l in "${orig[@]}"; do c="${l#\#}"; disp+=("${c% \[*\]}"); done
idx=$(printf '%s\n' "${disp[@]}" | rofi -dmenu -i -format i -p "Action" -theme ~/.config/rofi/notify.rasi)
[ -n "$idx" ] && printf '%s\n' "${orig[$idx]}"
